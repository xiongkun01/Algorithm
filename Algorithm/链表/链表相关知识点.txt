一、链表的分类
1、按照连接方向进行分类
单链表、双链表

2、按照有环和无环分类
普通链表、循环链表

二、链表问题代码实现的关键点
1、链表调整的返回值类型，根据要求往往是节点类型。
2、处理链表的过程中，先采用画图的方式理清逻辑。
3、链表问题对于边界条件讨论要求严格。

三、关于链表插入和删除的注意事项
1、特殊处理链表为空，或者链表长度为1的情况。
2、注意插入操作的调整过程。
3、注意删除操作的调整过程。
注意点：头尾节点及空节点需要特殊考虑。

四、单链表的翻转操作
1、当链表为空或者长度为1时，特殊处理。
2、对于一般情况如动画所示。

五、总结
1、为了查找链表的中间节点，可以通过快慢指针的方式，快指针每次走两步，慢指针每次走一步，
当快指针为空时，慢指针指向中间节点。初始时low = head; fast = head.next;
2、在排序的时候需要时间复杂度o(nlogn)，可以考虑归并排序。